name: Keep App Alive

on:
  schedule:
    - cron: '0 1,6,11,16,21 * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -s -o /dev/null -w "%{http_code}" https://placement-file-filter-ajobpq5bepmagnqbhsup3f.streamlit.app
